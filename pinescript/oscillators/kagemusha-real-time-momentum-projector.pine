//@version=5
indicator("影武者・噴火仕様", overlay=true)

// 基準を「今の値段(close)」に変更し、勢いを強調
momentum = ta.sma(close - open, 3) // より敏感に(5→3)
kagemusha_wing = close + (momentum * 2) // 勢いを2倍にして投影

// 3本先に「銀の翼」を出す
plot(kagemusha_wing, color=color.new(color.silver, 0), linewidth=2, offset=3, title="影武者")
