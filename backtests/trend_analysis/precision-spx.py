"""
DS-TV Backtest Results: precision-spx
============================================================

--- SPY ---
  Return                    0.00%
  Buy & Hold Return         41.94%
  Max Drawdown              -0.00%
  Sharpe Ratio              nan
  Sortino Ratio             nan
  Calmar Ratio              nan
  Profit Factor             nan
  Expectancy                nan%
  SQN                       nan
  # Trades                  0
  Win Rate                  nan%
  Best Trade                nan%
  Worst Trade               nan%
  Avg Trade                 nan%
  Exposure Time             0.00%
  Equity Final              100000.00$
  Equity Peak               100000.00$
  Avg Drawdown              nan%
  Max Drawdown Duration     nan
  Max Trade Duration        nan
  Avg Trade Duration        nan
  Ann. Return               0.00%
  Ann. Volatility           0.00%

--- BTC ---
  Return                    0.00%
  Buy & Hold Return         46.22%
  Max Drawdown              -0.00%
  Sharpe Ratio              nan
  Sortino Ratio             nan
  Calmar Ratio              nan
  Profit Factor             nan
  Expectancy                nan%
  SQN                       nan
  # Trades                  0
  Win Rate                  nan%
  Best Trade                nan%
  Worst Trade               nan%
  Avg Trade                 nan%
  Exposure Time             0.00%
  Equity Final              100000.00$
  Equity Peak               100000.00$
  Avg Drawdown              nan%
  Max Drawdown Duration     nan
  Max Trade Duration        nan
  Avg Trade Duration        nan
  Ann. Return               0.00%
  Ann. Volatility           0.00%

--- QQQ ---
  Return                    0.00%
  Buy & Hold Return         42.67%
  Max Drawdown              -0.00%
  Sharpe Ratio              nan
  Sortino Ratio             nan
  Calmar Ratio              nan
  Profit Factor             nan
  Expectancy                nan%
  SQN                       nan
  # Trades                  0
  Win Rate                  nan%
  Best Trade                nan%
  Worst Trade               nan%
  Avg Trade                 nan%
  Exposure Time             0.00%
  Equity Final              100000.00$
  Equity Peak               100000.00$
  Avg Drawdown              nan%
  Max Drawdown Duration     nan
  Max Trade Duration        nan
  Avg Trade Duration        nan
  Ann. Return               0.00%
  Ann. Volatility           0.00%

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — precision-spx
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    # Default level prices - can be configured
    d1_price = 0.0
    d2_price = 0.0
    d3_price = 0.0
    d4_price = 0.0
    d5_price = 0.0
    d6_price = 0.0
    dr1_price = 0.0
    dr2_price = 0.0
    w1_price = 0.0
    w2_price = 0.0
    m1_price = 0.0
    m2_price = 0.0

    def init(self):
        pass

    def next(self):
        close = self.data.Close[-1]
        
        # Track which levels have been triggered to avoid multiple trades
        if not hasattr(self, 'fired_levels'):
            self.fired_levels = set()
        
        # Check if we're in a new day for reset
        if len(self.data) > 1:
            current_date = pd.Timestamp(self.data.index[-1]).date()
            if not hasattr(self, 'last_date'):
                self.last_date = current_date
            elif self.last_date != current_date:
                self.fired_levels = set()
                self.last_date = current_date
        
        # Daily Level 1 (R2_Hi) - buy signal
        if self.d1_price != 0.0 and 'd1' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.d1_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('d1')
        
        # Daily Level 2 (R1_Hi) - buy signal
        if self.d2_price != 0.0 and 'd2' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.d2_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('d2')
        
        # Daily Level 3 (P_Hi) - buy signal
        if self.d3_price != 0.0 and 'd3' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.d3_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('d3')
        
        # Daily Level 4 (P_Lo) - sell signal
        if self.d4_price != 0.0 and 'd4' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.d4_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('d4')
        
        # Daily Level 5 (R1_Lo) - sell signal
        if self.d5_price != 0.0 and 'd5' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.d5_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('d5')
        
        # Daily Level 6 (R2_Lo) - sell signal
        if self.d6_price != 0.0 and 'd6' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.d6_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('d6')
        
        # Daily Range 1 (DR_Hi) - buy signal
        if self.dr1_price != 0.0 and 'dr1' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.dr1_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('dr1')
        
        # Daily Range 2 (DR_Lo) - sell signal
        if self.dr2_price != 0.0 and 'dr2' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.dr2_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('dr2')
        
        # Weekly Level 1 (W_Hi) - buy signal
        if self.w1_price != 0.0 and 'w1' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.w1_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('w1')
        
        # Weekly Level 2 (W_Lo) - sell signal
        if self.w2_price != 0.0 and 'w2' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.w2_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('w2')
        
        # Monthly Level 1 (M_Hi) - buy signal
        if self.m1_price != 0.0 and 'm1' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close <= self.m1_price < close:
                    if not self.position:
                        self.buy()
                    self.fired_levels.add('m1')
        
        # Monthly Level 2 (M_Lo) - sell signal
        if self.m2_price != 0.0 and 'm2' not in self.fired_levels:
            if len(self.data) > 1:
                prev_close = self.data.Close[-2]
                if prev_close >= self.m2_price > close:
                    if self.position:
                        self.sell()
                    self.fired_levels.add('m2')


if __name__ == '__main__':
    import yfinance as yf
    
    # Download sample data
    data = yf.download('SPY', start='2023-01-01', end='2024-01-01')
    
    # Create backtest instance with some example levels
    bt = Backtest(data, TvStrategy, cash=10000, commission=.002)
    
    # Set level prices (example values)
    bt.run().TvStrategy.d1_price = 450.0
    bt.run().TvStrategy.d2_price = 445.0
    bt.run().TvStrategy.d3_price = 440.0
    bt.run().TvStrategy.d4_price = 430.0
    bt.run().TvStrategy.d5_price = 425.0
    bt.run().TvStrategy.d6_price = 420.0
    
    stats = bt.run()
    print(stats)