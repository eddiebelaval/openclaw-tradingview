"""
DS-TV Backtest Results: webbys-simple-fibonacci-trend
============================================================

--- SPY ---
  ERROR: Indicator "calculate…" error. See traceback above.

--- BTC ---
  ERROR: Indicator "calculate…" error. See traceback above.

--- QQQ ---
  ERROR: Indicator "calculate…" error. See traceback above.

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — webbys-simple-fibonacci-trend
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    def init(self):
        close = pd.Series(self.data.Close)
        
        self.ma1 = self.I(pta.ema, close, length=3)
        self.ma2 = self.I(pta.ema, close, length=5)
        self.ma3 = self.I(pta.ema, close, length=8)
        self.ma4 = self.I(pta.ema, close, length=13)
        self.ma5 = self.I(pta.ema, close, length=21)
        self.ma6 = self.I(pta.ema, close, length=34)
        self.ma7 = self.I(pta.ema, close, length=55)
        self.ma8 = self.I(pta.ema, close, length=89)
        self.ma9 = self.I(pta.ema, close, length=144)
        self.ma10 = self.I(pta.ema, close, length=233)
        
        self.score = self.I(self.calculate_score)

    def calculate_score(self):
        score_series = pd.Series(index=self.data.Close.index, dtype=float)
        
        for i in range(len(self.data.Close)):
            m1 = self.ma1[i] if i < len(self.ma1) else np.nan
            m2 = self.ma2[i] if i < len(self.ma2) else np.nan
            m3 = self.ma3[i] if i < len(self.ma3) else np.nan
            m4 = self.ma4[i] if i < len(self.ma4) else np.nan
            m5 = self.ma5[i] if i < len(self.ma5) else np.nan
            m6 = self.ma6[i] if i < len(self.ma6) else np.nan
            m7 = self.ma7[i] if i < len(self.ma7) else np.nan
            m8 = self.ma8[i] if i < len(self.ma8) else np.nan
            m9 = self.ma9[i] if i < len(self.ma9) else np.nan
            m10 = self.ma10[i] if i < len(self.ma10) else np.nan
            
            if any(pd.isna(x) for x in [m1, m2, m3, m4, m5, m6, m7, m8, m9, m10]):
                score_series.iloc[i] = np.nan
                continue
            
            score = 0
            score += (1 if m1 > m2 else 0)
            score += (1 if m1 > m3 else 0)
            score += (1 if m1 > m4 else 0)
            score += (1 if m1 > m5 else 0)
            score += (1 if m1 > m6 else 0)
            score += (1 if m1 > m7 else 0)
            score += (1 if m1 > m8 else 0)
            score += (1 if m1 > m9 else 0)
            score += (1 if m1 > m10 else 0)
            
            score += (1 if m2 > m3 else 0)
            score += (1 if m2 > m4 else 0)
            score += (1 if m2 > m5 else 0)
            score += (1 if m2 > m6 else 0)
            score += (1 if m2 > m7 else 0)
            score += (1 if m2 > m8 else 0)
            score += (1 if m2 > m9 else 0)
            score += (1 if m2 > m10 else 0)
            
            score += (1 if m3 > m4 else 0)
            score += (1 if m3 > m5 else 0)
            score += (1 if m3 > m6 else 0)
            score += (1 if m3 > m7 else 0)
            score += (1 if m3 > m8 else 0)
            score += (1 if m3 > m9 else 0)
            score += (1 if m3 > m10 else 0)
            
            score += (1 if m4 > m5 else 0)
            score += (1 if m4 > m6 else 0)
            score += (1 if m4 > m7 else 0)
            score += (1 if m4 > m8 else 0)
            score += (1 if m4 > m9 else 0)
            score += (1 if m4 > m10 else 0)
            
            score += (1 if m5 > m6 else 0)
            score += (1 if m5 > m7 else 0)
            score += (1 if m5 > m8 else 0)
            score += (1 if m5 > m9 else 0)
            score += (1 if m5 > m10 else 0)
            
            score += (1 if m6 > m7 else 0)
            score += (1 if m6 > m8 else 0)
            score += (1 if m6 > m9 else 0)
            score += (1 if m6 > m10 else 0)
            
            score += (1 if m7 > m8 else 0)
            score += (1 if m7 > m9 else 0)
            score += (1 if m7 > m10 else 0)
            
            score += (1 if m8 > m9 else 0)
            score += (1 if m8 > m10 else 0)
            
            score += (1 if m9 > m10 else 0)
            
            score_series.iloc[i] = score
        
        return score_series

    def next(self):
        if pd.isna(self.score[-1]):
            return
        
        if not self.position:
            if self.score[-1] > 35:
                self.buy()
        else:
            if self.score[-1] < 30:
                self.sell()


if __name__ == "__main__":
    from backtesting.test import SMA, GOOG
    
    bt = Backtest(GOOG, TvStrategy, cash=10000, commission=.002)
    stats = bt.run()
    print(stats)
    bt.plot()