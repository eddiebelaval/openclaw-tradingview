"""
DS-TV Backtest Results: all-in-one-mtf-ema-x20
============================================================

--- SPY ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (500,); indicator "ema(800)" shape: , returned value: None)

--- BTC ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (731,); indicator "ema(800)" shape: , returned value: None)

--- QQQ ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (500,); indicator "ema(800)" shape: , returned value: None)

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — all-in-one-mtf-ema-x20
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    ema1_period = 20
    ema2_period = 50
    ema3_period = 100
    ema4_period = 200
    ema5_period = 450
    ema6_period = 9
    ema7_period = 12
    ema8_period = 26
    ema9_period = 55
    ema10_period = 800
    ema11_period = 21
    ema12_period = 34
    ema13_period = 89
    ema14_period = 144
    ema15_period = 233
    ema16_period = 377
    ema17_period = 610
    ema18_period = 987
    ema19_period = 1597
    ema20_period = 2584

    def init(self):
        close = pd.Series(self.data.Close)
        
        self.ema1 = self.I(pta.ema, close, length=self.ema1_period)
        self.ema2 = self.I(pta.ema, close, length=self.ema2_period)
        self.ema3 = self.I(pta.ema, close, length=self.ema3_period)
        self.ema4 = self.I(pta.ema, close, length=self.ema4_period)
        self.ema5 = self.I(pta.ema, close, length=self.ema5_period)
        self.ema6 = self.I(pta.ema, close, length=self.ema6_period)
        self.ema7 = self.I(pta.ema, close, length=self.ema7_period)
        self.ema8 = self.I(pta.ema, close, length=self.ema8_period)
        self.ema9 = self.I(pta.ema, close, length=self.ema9_period)
        self.ema10 = self.I(pta.ema, close, length=self.ema10_period)
        self.ema11 = self.I(pta.ema, close, length=self.ema11_period)
        self.ema12 = self.I(pta.ema, close, length=self.ema12_period)
        self.ema13 = self.I(pta.ema, close, length=self.ema13_period)
        self.ema14 = self.I(pta.ema, close, length=self.ema14_period)
        self.ema15 = self.I(pta.ema, close, length=self.ema15_period)
        self.ema16 = self.I(pta.ema, close, length=self.ema16_period)
        self.ema17 = self.I(pta.ema, close, length=self.ema17_period)
        self.ema18 = self.I(pta.ema, close, length=self.ema18_period)
        self.ema19 = self.I(pta.ema, close, length=self.ema19_period)
        self.ema20 = self.I(pta.ema, close, length=self.ema20_period)

    def next(self):
        close_price = self.data.Close[-1]
        
        fast_ema = self.ema1[-1]
        slow_ema = self.ema2[-1]
        
        if fast_ema is not None and slow_ema is not None:
            if crossover(self.ema1, self.ema2):
                if not self.position:
                    self.buy()
            elif crossover(self.ema2, self.ema1):
                if self.position:
                    self.position.close()


if __name__ == "__main__":
    import yfinance as yf
    
    data = yf.download("BTC-USD", start="2022-01-01", end="2023-12-31", progress=False)
    
    bt = Backtest(data, TvStrategy, cash=10000, commission=.002)
    stats = bt.run()
    print(stats)