"""
DS-TV Backtest Results: mad-6-bunches
============================================================

--- SPY ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (500,); indicator "ema(650)" shape: , returned value: None)

--- BTC ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (731,); indicator "ema(850)" shape: , returned value: None)

--- QQQ ---
  ERROR: Indicators must return (optionally a tuple of) numpy.arrays of same length as `data` (data shape: (500,); indicator "ema(650)" shape: , returned value: None)

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — mad-6-bunches
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    def init(self):
        close = pd.Series(self.data.Close)
        
        # Batch 1
        self.ema5 = self.I(pta.ema, close, length=5)
        self.ema6 = self.I(pta.ema, close, length=6)
        self.ema7 = self.I(pta.ema, close, length=7)
        
        # Batch 2
        self.ema11 = self.I(pta.ema, close, length=11)
        self.ema12 = self.I(pta.ema, close, length=12)
        self.ema13 = self.I(pta.ema, close, length=13)
        
        self.ema30 = self.I(pta.ema, close, length=30)
        self.ema33 = self.I(pta.ema, close, length=33)
        self.ema36 = self.I(pta.ema, close, length=36)
        self.ema39 = self.I(pta.ema, close, length=39)
        
        # Batch 3
        self.ema38 = self.I(pta.ema, close, length=38)
        self.ema42 = self.I(pta.ema, close, length=42)
        self.ema46 = self.I(pta.ema, close, length=46)
        self.ema50 = self.I(pta.ema, close, length=50)
        
        self.ema81 = self.I(pta.ema, close, length=81)
        self.ema90 = self.I(pta.ema, close, length=90)
        self.ema99 = self.I(pta.ema, close, length=99)
        self.ema108 = self.I(pta.ema, close, length=108)
        
        # Batch 4
        self.ema150 = self.I(pta.ema, close, length=150)
        self.ema165 = self.I(pta.ema, close, length=165)
        self.ema180 = self.I(pta.ema, close, length=180)
        self.ema195 = self.I(pta.ema, close, length=195)
        self.ema210 = self.I(pta.ema, close, length=210)
        
        # Batch 5
        self.ema330 = self.I(pta.ema, close, length=330)
        self.ema360 = self.I(pta.ema, close, length=360)
        self.ema390 = self.I(pta.ema, close, length=390)
        self.ema420 = self.I(pta.ema, close, length=420)
        
        # Batch 6
        self.ema650 = self.I(pta.ema, close, length=650)
        self.ema850 = self.I(pta.ema, close, length=850)
        self.ema3000 = self.I(pta.ema, close, length=3000)
        self.ema3900 = self.I(pta.ema, close, length=3900)
        self.ema4995 = self.I(pta.ema, close, length=4995)

    def next(self):
        # Use Batch 2 as fastEMA (ema39 for non-1m charts)
        # Use Batch 3 as slowEMA (ema108 for non-1m charts)
        fastEMA = self.ema39
        slowEMA = self.ema108
        
        # Buy signal: fastEMA crosses above slowEMA
        if crossover(fastEMA, slowEMA):
            if not self.position:
                self.buy()
        
        # Sell signal: fastEMA crosses below slowEMA
        elif crossover(slowEMA, fastEMA):
            if self.position:
                self.position.close()


if __name__ == "__main__":
    import yfinance as yf
    
    # Download sample data
    data = yf.download("AAPL", start="2022-01-01", end="2023-01-01", progress=False)
    
    # Run backtest
    bt = Backtest(data, TvStrategy, cash=10000, commission=.002)
    stats = bt.run()
    print(stats)
    bt.plot()