"""
DS-TV Backtest Results: ema-rainbow-3-bands-9-emas
============================================================

--- SPY ---
  ERROR: object of type 'TvStrategy' has no len()

--- BTC ---
  ERROR: object of type 'TvStrategy' has no len()

--- QQQ ---
  ERROR: object of type 'TvStrategy' has no len()

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — ema-rainbow-3-bands-9-emas
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    b1_start = 20
    b1_end = 60
    b2_start = 60
    b2_end = 120
    b3_start = 120
    b3_end = 300

    def init(self):
        close = pd.Series(self.data.Close)
        
        # Calculate step sizes
        b1_step = (self.b1_end - self.b1_start) / 8.0
        b2_step = (self.b2_end - self.b2_start) / 8.0
        b3_step = (self.b3_end - self.b3_start) / 8.0
        
        # Band 1 EMAs
        self.b1_1 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 0))
        self.b1_2 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 1))
        self.b1_3 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 2))
        self.b1_4 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 3))
        self.b1_5 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 4))
        self.b1_6 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 5))
        self.b1_7 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 6))
        self.b1_8 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 7))
        self.b1_9 = self.I(pta.ema, close, length=int(self.b1_start + b1_step * 8))
        
        # Band 2 EMAs
        self.b2_1 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 0))
        self.b2_2 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 1))
        self.b2_3 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 2))
        self.b2_4 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 3))
        self.b2_5 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 4))
        self.b2_6 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 5))
        self.b2_7 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 6))
        self.b2_8 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 7))
        self.b2_9 = self.I(pta.ema, close, length=int(self.b2_start + b2_step * 8))
        
        # Band 3 EMAs
        self.b3_1 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 0))
        self.b3_2 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 1))
        self.b3_3 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 2))
        self.b3_4 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 3))
        self.b3_5 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 4))
        self.b3_6 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 5))
        self.b3_7 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 6))
        self.b3_8 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 7))
        self.b3_9 = self.I(pta.ema, close, length=int(self.b3_start + b3_step * 8))

    def next(self):
        # Buy when price crosses above the first band anchor (B1-9, the longest EMA in band 1)
        # Sell when price crosses below the third band anchor (B3-9, the longest EMA in band 3)
        
        if len(self) < 2:
            return
        
        # Buy signal: price crosses above B1-9 (support/trend reversal)
        if crossover(self.data.Close, self.b1_9):
            if not self.position:
                self.buy()
        
        # Sell signal: price crosses below B3-9 (resistance/trend reversal)
        elif crossover(self.b3_9, self.data.Close):
            if self.position:
                self.position.close()