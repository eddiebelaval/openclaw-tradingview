"""
DS-TV Backtest Results: all-in-one-vwaps
============================================================

--- SPY ---
  ERROR: Indicator "_calc_wee…" error. See traceback above.

--- BTC ---
  ERROR: Indicator "_calc_wee…" error. See traceback above.

--- QQQ ---
  ERROR: Indicator "_calc_wee…" error. See traceback above.

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — all-in-one-vwaps
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    def init(self):
        close = pd.Series(self.data.Close)
        high = pd.Series(self.data.High)
        low = pd.Series(self.data.Low)
        volume = pd.Series(self.data.Volume)
        
        hlc3 = (high + low + close) / 3
        
        self.sess_vwap = self.I(self._calc_session_vwap, hlc3, volume)
        self.week_vwap = self.I(self._calc_weekly_vwap, hlc3, volume)
        self.month_vwap = self.I(self._calc_monthly_vwap, hlc3, volume)
        self.ny_vwap = self.I(self._calc_ny_vwap, hlc3, volume)
        self.ldn_vwap = self.I(self._calc_london_vwap, hlc3, volume)
        self.asia_vwap = self.I(self._calc_asia_vwap, hlc3, volume)
        self.anchor_a_vwap = self.I(self._calc_anchor_a_vwap, hlc3, volume)
        self.anchor_b_vwap = self.I(self._calc_anchor_b_vwap, hlc3, volume)

    def _calc_session_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        
        for i in range(len(src)):
            cum_pv += volume.iloc[i] * src.iloc[i]
            cum_v += volume.iloc[i]
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
        
        return pd.Series(result, index=src.index)

    def _calc_weekly_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_week = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_week = idx.isocalendar()[1]
            current_year = idx.year
            
            if last_week is not None and (current_week != last_week or current_year != last_year):
                cum_pv = 0.0
                cum_v = 0.0
            
            cum_pv += volume.iloc[i] * src.iloc[i]
            cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_week = current_week
            last_year = current_year
        
        return pd.Series(result, index=src.index)

    def _calc_monthly_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_month = None
        last_year = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_month = idx.month
            current_year = idx.year
            
            if last_month is not None and (current_month != last_month or current_year != last_year):
                cum_pv = 0.0
                cum_v = 0.0
            
            cum_pv += volume.iloc[i] * src.iloc[i]
            cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_month = current_month
            last_year = current_year
        
        return pd.Series(result, index=src.index)

    def _calc_ny_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_day = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_day = idx.date()
            current_hour = idx.hour
            
            if last_day is not None and current_day != last_day:
                cum_pv = 0.0
                cum_v = 0.0
            
            if current_hour >= 9:
                cum_pv += volume.iloc[i] * src.iloc[i]
                cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_day = current_day
        
        return pd.Series(result, index=src.index)

    def _calc_london_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_day = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_day = idx.date()
            current_hour = idx.hour
            
            if last_day is not None and current_day != last_day:
                cum_pv = 0.0
                cum_v = 0.0
            
            if current_hour >= 2:
                cum_pv += volume.iloc[i] * src.iloc[i]
                cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_day = current_day
        
        return pd.Series(result, index=src.index)

    def _calc_asia_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_day = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_day = idx.date()
            current_hour = idx.hour
            
            if last_day is not None and current_day != last_day:
                cum_pv = 0.0
                cum_v = 0.0
            
            if current_hour >= 19:
                cum_pv += volume.iloc[i] * src.iloc[i]
                cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_day = current_day
        
        return pd.Series(result, index=src.index)

    def _calc_anchor_a_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_day = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_day = idx.date()
            current_hour = idx.hour
            
            if last_day is not None and current_day != last_day:
                cum_pv = 0.0
                cum_v = 0.0
            
            if current_hour >= 9:
                cum_pv += volume.iloc[i] * src.iloc[i]
                cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_day = current_day
        
        return pd.Series(result, index=src.index)

    def _calc_anchor_b_vwap(self, src, volume):
        cum_pv = 0.0
        cum_v = 0.0
        result = []
        last_day = None
        
        for i in range(len(src)):
            idx = src.index[i]
            current_day = idx.date()
            current_hour = idx.hour
            current_min = idx.minute
            
            if last_day is not None and current_day != last_day:
                cum_pv = 0.0
                cum_v = 0.0
            
            if current_hour > 8 or (current_hour == 8 and current_min >= 30):
                cum_pv += volume.iloc[i] * src.iloc[i]
                cum_v += volume.iloc[i]
            
            if cum_v > 0:
                result.append(cum_pv / cum_v)
            else:
                result.append(np.nan)
            
            last_day = current_day
        
        return pd.Series(result, index=src.index)

    def next(self):
        if len(self) < 2:
            return
        
        close = self.data.Close[-1]
        
        sess_vwap_val = self.sess_vwap[-1] if not np.isnan(self.sess_vwap[-1]) else None
        week_vwap_val = self.week_vwap[-1] if not np.isnan(self.week_vwap[-1]) else None
        
        if sess_vwap_val is not None:
            if close > sess_vwap_val and self.data.Close[-2] <= self.sess_vwap[-2]:
                if not self.position:
                    self.buy()
            elif close < sess_vwap_val and self.data.Close[-2] >= self.sess_vwap[-2]:
                if self.position:
                    self.position.close()
        
        if week_vwap_val is not None:
            if close < week_vwap_val and self.data.Close[-2] >= self.week_vwap[-2]:
                if self.position:
                    self.position.close()