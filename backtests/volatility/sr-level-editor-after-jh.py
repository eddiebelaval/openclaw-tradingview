"""
DS-TV Backtest Results: sr-level-editor-after-jh
============================================================

--- SPY ---
  ERROR: object of type 'TvStrategy' has no len()

--- BTC ---
  ERROR: object of type 'TvStrategy' has no len()

--- QQQ ---
  ERROR: object of type 'TvStrategy' has no len()

============================================================
Unoptimized run. No parameter tuning applied.

Source: TradingView Community Script — sr-level-editor-after-jh
Generated by DeepStack TradingView Pipeline
"""

import numpy as np
import pandas as pd
import pandas_ta as pta
from backtesting import Backtest, Strategy
from backtesting.lib import crossover


class TvStrategy(Strategy):
    s1 = 0.0
    s2 = 0.0
    s3 = 0.0
    r1 = 0.0
    r2 = 0.0
    r3 = 0.0

    def init(self):
        close = pd.Series(self.data.Close)
        
        self.cross_s1 = self.I(lambda: pd.Series([
            1 if (self.s1 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.s1 and self.data.Close[i] > self.s1) or
                   (self.data.Close[i-1] >= self.s1 and self.data.Close[i] < self.s1))) else 0
            for i in range(len(self.data.Close))
        ]))
        
        self.cross_s2 = self.I(lambda: pd.Series([
            1 if (self.s2 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.s2 and self.data.Close[i] > self.s2) or
                   (self.data.Close[i-1] >= self.s2 and self.data.Close[i] < self.s2))) else 0
            for i in range(len(self.data.Close))
        ]))
        
        self.cross_s3 = self.I(lambda: pd.Series([
            1 if (self.s3 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.s3 and self.data.Close[i] > self.s3) or
                   (self.data.Close[i-1] >= self.s3 and self.data.Close[i] < self.s3))) else 0
            for i in range(len(self.data.Close))
        ]))
        
        self.cross_r1 = self.I(lambda: pd.Series([
            1 if (self.r1 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.r1 and self.data.Close[i] > self.r1) or
                   (self.data.Close[i-1] >= self.r1 and self.data.Close[i] < self.r1))) else 0
            for i in range(len(self.data.Close))
        ]))
        
        self.cross_r2 = self.I(lambda: pd.Series([
            1 if (self.r2 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.r2 and self.data.Close[i] > self.r2) or
                   (self.data.Close[i-1] >= self.r2 and self.data.Close[i] < self.r2))) else 0
            for i in range(len(self.data.Close))
        ]))
        
        self.cross_r3 = self.I(lambda: pd.Series([
            1 if (self.r3 != 0.0 and 
                  i > 0 and 
                  ((self.data.Close[i-1] <= self.r3 and self.data.Close[i] > self.r3) or
                   (self.data.Close[i-1] >= self.r3 and self.data.Close[i] < self.r3))) else 0
            for i in range(len(self.data.Close))
        ]))

    def next(self):
        if len(self) < 2:
            return
        
        close_prev = self.data.Close[-2]
        close_curr = self.data.Close[-1]
        
        any_touch = False
        touch_below = False
        touch_above = False
        
        if self.s1 != 0.0:
            if (close_prev <= self.s1 and close_curr > self.s1) or (close_prev >= self.s1 and close_curr < self.s1):
                any_touch = True
                if close_curr < self.s1:
                    touch_below = True
        
        if self.s2 != 0.0:
            if (close_prev <= self.s2 and close_curr > self.s2) or (close_prev >= self.s2 and close_curr < self.s2):
                any_touch = True
                if close_curr < self.s2:
                    touch_below = True
        
        if self.s3 != 0.0:
            if (close_prev <= self.s3 and close_curr > self.s3) or (close_prev >= self.s3 and close_curr < self.s3):
                any_touch = True
                if close_curr < self.s3:
                    touch_below = True
        
        if self.r1 != 0.0:
            if (close_prev <= self.r1 and close_curr > self.r1) or (close_prev >= self.r1 and close_curr < self.r1):
                any_touch = True
                if close_curr > self.r1:
                    touch_above = True
        
        if self.r2 != 0.0:
            if (close_prev <= self.r2 and close_curr > self.r2) or (close_prev >= self.r2 and close_curr < self.r2):
                any_touch = True
                if close_curr > self.r2:
                    touch_above = True
        
        if self.r3 != 0.0:
            if (close_prev <= self.r3 and close_curr > self.r3) or (close_prev >= self.r3 and close_curr < self.r3):
                any_touch = True
                if close_curr > self.r3:
                    touch_above = True
        
        if touch_below and not self.position:
            self.buy()
        elif touch_above and self.position:
            self.sell()
        elif not any_touch and self.position and close_curr < self.data.Close[-2]:
            self.sell()